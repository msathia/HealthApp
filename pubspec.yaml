name: HealthApp
description: A Flutter project to create a health app

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0 # Add this line

  english_words: ^4.0.0
  provider: ^6.0.0
  intl: ^0.18.0 # Add this line
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
